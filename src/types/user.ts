export type UserType = {
  email: string;
  firstName: string;
  hasPayment: boolean;
  lastLoginDate: string;
  lastName: string;
  otherNames: string;
  phone: string;
  role: string;
  status: string;
  token: string;
};

export type UncertainObjectType = {
  [key: string]: any;
};
